# Test Fixtures

This directory contains test data and fixtures used across tests.

## Structure

- `users.json` - Sample user data for tests
- `tasks.json` - Sample task data for tests
- `time-entries.json` - Sample time entry data for tests

## Usage

```typescript
import users from '@/tests/fixtures/users.json';
```
